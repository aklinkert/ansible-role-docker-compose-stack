galaxy_info:
  role_name: docker_compose_stack
  author: Alex Klinkert
  description: Role to configure a docker-compose stack with systemd on Debian/Ubuntu.
  license: Apache v2.0
  min_ansible_version: 2.5
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all
  galaxy_tags:
    - docker
    - docker compose
    - stack
    - systemd
    - systemd service
    - docker compose stack
dependencies: []
